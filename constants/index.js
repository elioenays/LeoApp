import assets from './assets';
import { ProjectData } from './dummy';
import { COLORS, FONTS, SHADOWS, SIZES, styles } from './theme';

export { assets, COLORS, SHADOWS, SIZES, FONTS, ProjectData, styles };
